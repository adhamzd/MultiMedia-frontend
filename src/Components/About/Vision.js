import React from "react";
const Vision = ({ description }) => {
  return (
    <div className="vision">
      <p className="description">{`OUR VISION: ${description}`}</p>
    </div>
  );
};

export default Vision;
